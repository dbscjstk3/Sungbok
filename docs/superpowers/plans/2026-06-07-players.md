# Players 참가자 명단 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** InsForge Postgres에 `players` 테이블을 만들고, `/players` 페이지에서 참가자 목록 조회 및 등록이 가능하게 한다.

**Architecture:** InsForge SDK(`@insforge/sdk`)로 브라우저에서 직접 DB를 읽고 쓴다. `/players`는 전체 클라이언트 컴포넌트로, 마운트 시 목록을 가져오고 폼으로 새 참가자를 추가한다. RLS는 `anon` 역할에 SELECT/INSERT를 허용한다 (인증 없음).

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind CSS v4, @insforge/sdk, InsForge Postgres

---

### Task 1: 환경 변수 설정 및 SDK 설치

**Files:**
- Create: `.env.local`
- Modify: `package.json` (npm install로 자동 갱신)

- [ ] **Step 1: InsForge 프로젝트 연결 확인**

```bash
npx @insforge/cli current
```

Expected output: `Sungbok` 프로젝트가 연결되어 있음을 확인

- [ ] **Step 2: ANON_KEY 가져오기**

```bash
npx @insforge/cli secrets get ANON_KEY
```

출력된 값을 복사해 둔다.

- [ ] **Step 3: `.env.local` 작성**

`.insforge/project.json`의 `oss_host` 값과 위에서 얻은 ANON_KEY로:

```bash
# .env.local
NEXT_PUBLIC_INSFORGE_URL=https://4h9mkzd9.ap-southeast.insforge.app
NEXT_PUBLIC_INSFORGE_ANON_KEY=<Step 2에서 얻은 값>
```

- [ ] **Step 4: SDK 설치**

```bash
npm install @insforge/sdk@latest
```

Expected: `package.json`의 `dependencies`에 `@insforge/sdk` 추가

- [ ] **Step 5: 커밋**

```bash
git add package.json package-lock.json
git commit -m "chore: install @insforge/sdk"
```

> `.env.local`은 `.gitignore`에 이미 있으므로 커밋하지 않는다. 없다면 추가한다.

---

### Task 2: 데이터베이스 마이그레이션

**Files:**
- Create: `migrations/YYYYMMDDHHMMSS_create-players.sql` (CLI가 생성)

- [ ] **Step 1: 현재 DB 상태 확인**

```bash
npx @insforge/cli db tables
npx @insforge/cli db migrations list
```

`players` 테이블이 없음을 확인.

- [ ] **Step 2: 마이그레이션 파일 생성**

```bash
npx @insforge/cli db migrations new create-players
```

Expected: `migrations/YYYYMMDDHHMMSS_create-players.sql` 파일 생성

- [ ] **Step 3: 마이그레이션 파일 작성**

생성된 파일에 아래 SQL을 작성한다:

```sql
CREATE TABLE players (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  real_name TEXT NOT NULL,
  summoner_name TEXT UNIQUE NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE players ENABLE ROW LEVEL SECURITY;

CREATE POLICY "anyone can read players" ON players
  FOR SELECT TO anon, authenticated
  USING (true);

CREATE POLICY "anyone can add player" ON players
  FOR INSERT TO anon, authenticated
  WITH CHECK (true);

GRANT USAGE ON SCHEMA public TO anon, authenticated;
GRANT SELECT, INSERT ON players TO anon, authenticated;
```

- [ ] **Step 4: 마이그레이션 적용**

```bash
npx @insforge/cli db migrations up --all
```

Expected: `Applied 1 migration(s)` 또는 유사한 성공 메시지

- [ ] **Step 5: 테이블 확인**

```bash
npx @insforge/cli db tables
npx @insforge/cli db policies
```

Expected: `players` 테이블과 두 개의 RLS 정책이 보임

- [ ] **Step 6: 커밋**

```bash
git add migrations/
git commit -m "feat: add players table migration"
```

---

### Task 3: InsForge 클라이언트 모듈

**Files:**
- Create: `lib/insforge.ts`

- [ ] **Step 1: `lib/` 디렉토리 생성 및 클라이언트 파일 작성**

`lib/insforge.ts`:

```typescript
import { createClient } from '@insforge/sdk'

export const insforge = createClient({
  baseUrl: process.env.NEXT_PUBLIC_INSFORGE_URL!,
  anonKey: process.env.NEXT_PUBLIC_INSFORGE_ANON_KEY!,
})

export interface Player {
  id: string
  real_name: string
  summoner_name: string
  created_at: string
}
```

- [ ] **Step 2: 타입 체크**

```bash
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 3: 커밋**

```bash
git add lib/insforge.ts
git commit -m "feat: add insforge client module"
```

---

### Task 4: /players 페이지 구현

**Files:**
- Create: `app/players/page.tsx`

- [ ] **Step 1: `app/players/page.tsx` 작성**

```typescript
'use client'

import { useEffect, useState } from 'react'
import { insforge, Player } from '@/lib/insforge'

export default function PlayersPage() {
  const [players, setPlayers] = useState<Player[]>([])
  const [realName, setRealName] = useState('')
  const [summonerName, setSummonerName] = useState('')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  async function fetchPlayers() {
    const { data } = await insforge.database
      .from('players')
      .select('id, real_name, summoner_name, created_at')
      .order('created_at', { ascending: true })
    if (data) setPlayers(data as Player[])
  }

  useEffect(() => { fetchPlayers() }, [])

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    setError('')
    setLoading(true)

    const { error: insertError } = await insforge.database
      .from('players')
      .insert([{ real_name: realName.trim(), summoner_name: summonerName.trim() }])
      .select()

    setLoading(false)

    if (insertError) {
      if (insertError.message?.includes('unique') || insertError.message?.includes('duplicate')) {
        setError('이미 등록된 소환사명입니다.')
      } else {
        setError('등록 중 오류가 발생했습니다.')
      }
      return
    }

    setRealName('')
    setSummonerName('')
    fetchPlayers()
  }

  return (
    <main className="min-h-screen px-6 py-16 max-w-2xl mx-auto" style={{ backgroundColor: '#ECEEF0', color: '#202020' }}>
      <a href="/" className="text-sm hover:underline" style={{ color: '#202020', opacity: 0.5 }}>
        ← 홈으로
      </a>

      <h1 className="text-3xl font-bold mt-8 mb-2" style={{ color: '#202020' }}>
        참가자 명단
      </h1>
      <p className="text-sm mb-10" style={{ color: '#202020', opacity: 0.5 }}>
        {players.length}명 등록됨
      </p>

      {/* 목록 */}
      <section className="mb-12">
        {players.length === 0 ? (
          <p className="text-sm" style={{ color: '#202020', opacity: 0.4 }}>
            아직 등록된 참가자가 없습니다.
          </p>
        ) : (
          <ul className="flex flex-col gap-3">
            {players.map((p) => (
              <li
                key={p.id}
                className="flex justify-between items-center px-5 py-4 rounded-xl"
                style={{ backgroundColor: '#DEE0E2' }}
              >
                <span className="font-medium" style={{ color: '#202020' }}>
                  {p.real_name}
                </span>
                <span className="text-sm" style={{ color: '#202020', opacity: 0.55 }}>
                  {p.summoner_name}
                </span>
              </li>
            ))}
          </ul>
        )}
      </section>

      {/* 등록 폼 */}
      <section>
        <h2 className="text-lg font-bold mb-5" style={{ color: '#202020' }}>
          참가자 등록
        </h2>
        <form onSubmit={handleSubmit} className="flex flex-col gap-3">
          <input
            type="text"
            placeholder="실명 (예: 윤현석)"
            value={realName}
            onChange={(e) => setRealName(e.target.value)}
            required
            className="px-4 py-3 rounded-xl text-sm outline-none w-full"
            style={{ backgroundColor: '#DEE0E2', color: '#202020', border: 'none' }}
          />
          <input
            type="text"
            placeholder="소환사명 (예: Hide on bush)"
            value={summonerName}
            onChange={(e) => setSummonerName(e.target.value)}
            required
            className="px-4 py-3 rounded-xl text-sm outline-none w-full"
            style={{ backgroundColor: '#DEE0E2', color: '#202020', border: 'none' }}
          />
          {error && (
            <p className="text-sm" style={{ color: '#e53e3e' }}>
              {error}
            </p>
          )}
          <button
            type="submit"
            disabled={loading}
            className="px-6 py-3 rounded-xl text-sm font-semibold transition-opacity hover:opacity-85 disabled:opacity-40"
            style={{ backgroundColor: '#202020', color: '#ECEEF0' }}
          >
            {loading ? '등록 중...' : '등록하기'}
          </button>
        </form>
      </section>
    </main>
  )
}
```

- [ ] **Step 2: 타입 체크**

```bash
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 3: 커밋**

```bash
git add app/players/page.tsx
git commit -m "feat: add players page with list and registration form"
```

---

### Task 5: 수동 검증

- [ ] **Step 1: 개발 서버 실행**

```bash
npm run dev
```

- [ ] **Step 2: `/players` 페이지 접속**

브라우저에서 `http://localhost:3000/players` 열기.

Expected: 빈 목록과 등록 폼이 보임

- [ ] **Step 3: 참가자 등록 테스트**

실명 `윤현석`, 소환사명 `Hide on bush` 입력 후 등록.

Expected: 폼이 초기화되고 목록에 추가됨

- [ ] **Step 4: 중복 등록 테스트**

같은 소환사명으로 다시 등록 시도.

Expected: "이미 등록된 소환사명입니다." 에러 메시지 표시

- [ ] **Step 5: DB에서 직접 확인**

```bash
npx @insforge/cli db query "SELECT real_name, summoner_name, created_at FROM players ORDER BY created_at"
```

Expected: 방금 등록한 데이터가 보임
